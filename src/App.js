/** @format */

import './App.css';
import TodoApp from './Component/TodoApp';

const App = () => {
	return (
		<div className='App'>
			<h1>todoList App - Amihossein</h1>
			<TodoApp />
		</div>
	);
};

export default App;

// TodoApp : todos
// TodoForm => input + button =>
// TodoList => todos.map(...) =>
